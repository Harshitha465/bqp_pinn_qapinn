# Core deep learning
torch>=2.1.0

# Quantum computing
pennylane==0.45.1

# JAX — PINNED: PennyLane 0.45.x uses jax.core.is_concrete (removed in JAX 0.10+)
# Do NOT upgrade without verifying PennyLane compatibility first
jax==0.9.2
jaxlib==0.9.2

# Scientific computing
# Keep NumPy on a modern, secure 1.26.x line for compatibility with JAX 0.9.2/PennyLane 0.45.x
numpy>=1.26.4,<2.0
scipy
sympy

# Data & visualization
matplotlib
pandas
seaborn
